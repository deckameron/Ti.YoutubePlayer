<?xml version="1.0" encoding="UTF-8"?>
<ti:module xmlns:ti="http://ti.tidev.io" xmlns:android="http://schemas.android.com/apk/res/android">
	<!--
		Similar to tiapp.xml, but contains module/platform specific
		configuration in <iphone> and <android> sections
	-->
	<android xmlns:android="http://schemas.android.com/apk/res/android">
        <manifest>
            <!-- The player only needs network access. Camera/microphone/bluetooth
                 permissions were merged into every host app and trigger the Play
                 Console sensitive-permission declaration for no reason. -->
            <uses-permission android:name="android.permission.INTERNET"/>
            <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

            <application android:hardwareAccelerated="true">
            </application>
        </manifest>
    </android>
</ti:module>